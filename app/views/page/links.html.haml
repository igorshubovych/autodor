%h1= t('links.head')

- @links.each do |l|
	.commentari
		.commentari_img
			%a{ :href => "#{ l.url }", :target => '_blank', :title => t( l.title ) }
				=image_tag "/images/sites/#{ l.img }"

		.commentari_text
			= t( l.title )
